const INPUT_SHELL_CLASS = "rf-input-shell";
const INPUT_BASE_CLASS = "rf-input";

export const inputStyles = {
  shell: INPUT_SHELL_CLASS,
  base: INPUT_BASE_CLASS,
  searchShell: `${INPUT_SHELL_CLASS} rf-input-shell-search`,
} as const;
